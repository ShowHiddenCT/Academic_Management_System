@model IEnumerable<WebApplication3.Models.tb_stucourse>

@{
    ViewBag.Title = "Index";
}

<h2>课程管理</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tb_course.CourseName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tb_student.StudentPasswd)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tb_teacher.TeacherPasswd)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Grade)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tb_course.CourseName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tb_student.StudentPasswd)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tb_teacher.TeacherPasswd)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Grade)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
