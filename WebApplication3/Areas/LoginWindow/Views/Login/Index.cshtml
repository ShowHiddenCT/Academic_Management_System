@model IEnumerable<WebApplication3.Models.tb_Admin>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AdminPasswd)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AdminPasswd)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AdminNo }) |
            @Html.ActionLink("Details", "Details", new { id=item.AdminNo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AdminNo })
        </td>
    </tr>
}

</table>
